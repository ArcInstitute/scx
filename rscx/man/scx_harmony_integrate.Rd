% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmony.R
\name{scx_harmony_integrate}
\alias{scx_harmony_integrate}
\title{Harmony2 batch integration}
\arguments{
\item{embeddings}{Numeric matrix of shape \verb{N x d} (cells x PCs). Any
storage mode that coerces to \code{double} via \code{as.matrix} is accepted.}

\item{batch}{Character vector (or factor coerced with \code{as.character})
of length \code{N} with one batch label per cell. Factorised internally.}

\item{n_clusters}{Number of soft clusters \code{K} (integer). \code{NULL} uses
\code{min(N/30, 100)} clamped to \verb{[2, N/2]}.}

\item{theta}{Diversity-penalty strength. Scalar; default \code{2.0}.}

\item{sigma}{Soft-assignment kernel bandwidth. Scalar; default \code{0.1}.}

\item{lambda}{Ridge penalty. \code{NULL} (default) enables dynamic
estimation (\code{alpha * E[k, b]}). Supply a non-negative scalar to use
a fixed penalty for every batch level.}

\item{alpha}{Dynamic-lambda scale factor. Default \code{0.2}.}

\item{max_iter}{Maximum Harmony iterations. Default \code{10L}.}

\item{max_iter_kmeans}{Maximum k-means sub-iterations per Harmony
iteration. Default \code{6L} (must be >= 2*window_size for the convergence
check to fire).}

\item{epsilon_harmony}{Harmony convergence tolerance (signed relative
change). Default \code{1e-2}.}

\item{epsilon_kmeans}{K-means convergence tolerance (absolute relative
window change). Default \code{1e-3}.}

\item{block_size}{Stochastic block size as a fraction of \code{N}. Default
\code{0.05}.}

\item{batch_prop_cutoff}{Minimum batch proportion per cluster to apply
correction. Default \code{1e-5}.}

\item{tau}{Overcorrection protection — scales theta per batch size.
\code{0} (default) disables scaling; larger values protect smaller
batches.}

\item{random_state}{RNG seed (integer). Default \code{0L}.}
}
\value{
A named list:
\itemize{
\item \code{embeddings}: corrected \verb{N x d} numeric matrix.
\item \code{converged}: logical.
\item \code{n_iterations}: integer.
\item \code{n_clusters}: integer — the resolved \code{K}.
\item \code{objective}: numeric vector of per-iteration Harmony objectives.
}
}
\description{
Corrects batch effects in PCA-style embeddings using a clean-room Rust
implementation of the Harmony2 algorithm (Korsunsky et al.). The core
is \code{scx_accel::harmony_integrate} in the \code{scx-accel} Rust crate; this
is the R front end.
}
\details{
Defaults match those in the Python binding
(\code{pyscx.accel.harmony_integrate}) and the scanpy-external API.
}
\examples{
\dontrun{
set.seed(0)
N <- 400; d <- 10
pca <- matrix(rnorm(N * d), nrow = N, ncol = d)
batch <- sample(c("A", "B"), N, replace = TRUE)
res <- scx_harmony_integrate(pca, batch, max_iter = 5L)
str(res)
}
}
