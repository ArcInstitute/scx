% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lisi.R
\name{scx_compute_lisi}
\alias{scx_compute_lisi}
\title{Local Inverse Simpson Index (LISI)}
\arguments{
\item{embeddings}{Numeric matrix (N rows × d columns).}

\item{labels}{Character vector (or factor coerced with \code{as.character})
of length \code{N}.}

\item{perplexity}{Gaussian-kernel target perplexity (default 30).}

\item{n_neighbors}{Number of neighbours to use. \code{NULL} uses
\code{ceil(3 * perplexity)}.}
}
\value{
Numeric vector of length \code{N} with LISI values.
}
\description{
Computes the per-cell LISI metric (Korsunsky et al., 2019) on a
numeric embedding matrix with categorical labels. LISI approaches 1
when neighbourhoods share a single label (poor mixing) and approaches
the number of categories under uniform mixing (good mixing). Useful
as a batch-integration QC summary before and after running
\code{\link[=scx_harmony_integrate]{scx_harmony_integrate()}}.
}
\examples{
\dontrun{
set.seed(0)
N <- 300; d <- 10
emb <- matrix(rnorm(N * d), nrow = N, ncol = d)
lab <- sample(c("A", "B"), N, replace = TRUE)
mean(scx_compute_lisi(emb, lab))
}
}
