% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lazy.R
\name{scx-lazy-transforms}
\alias{scx-lazy-transforms}
\alias{scx_normalize_total}
\alias{scx_log1p}
\alias{scx_row_scale}
\title{Lazy transform verbs}
\usage{
scx_normalize_total(x, target_sum = 10000)

scx_log1p(x)

scx_row_scale(x, factors)
}
\arguments{
\item{x}{An \code{ScxLazyTransformed} (from \code{\link{scx_lazy_transform}}
or \code{exp$x_lazy()}).}

\item{target_sum}{Per-cell target count for \code{scx_normalize_total}
(default \code{1e4}, matching scanpy / pyscx). The per-cell sums used are
those of the data as transformed by the chain so far.}

\item{factors}{Per-cell numeric scaling vector for \code{scx_row_scale}
(length \code{nrow(x)}).}
}
\value{
A new \code{ScxLazyTransformed} with the transform appended.
}
\description{
Append a preprocessing transform to a lazy chain. Each returns a \strong{new}
\code{ScxLazyTransformed} (the input is unchanged), so they compose with the
pipe. Transforms are applied on read, not eagerly.
}
