% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backed.R
\name{scx_backed_sparse}
\alias{scx_backed_sparse}
\title{Open a backed (lazy) sparse view of an SCX file's X matrix}
\usage{
scx_backed_sparse(path, cache_shards = 128)
}
\arguments{
\item{path}{Path to the \code{.scx} file.}

\item{cache_shards}{Number of decoded CSR shards to keep in the LRU cache
(default 128). Larger values trade memory for fewer re-decodes on repeated
or sequential access.}
}
\value{
An \code{ScxBackedSparse} object.
}
\description{
Returns an \code{ScxBackedSparse} that reads rows from disk on demand with a
shard-level LRU cache, instead of materialising the whole matrix in memory
(\code{\link{scx_open}}\code{(path)$x_matrix()}). Index it like a matrix:
\code{bsd[1:100, ]} returns a \code{dgCMatrix} of the requested cells (rows)
by all genes (columns). This is the R equivalent of pyscx's
\code{to_anndata(backed=TRUE)} X.
}
\examples{
\dontrun{
bsd <- scx_backed_sparse("experiment.scx")
dim(bsd)
first100 <- bsd[1:100, ]      # dgCMatrix, cells x genes
subset <- bsd[c(5, 1, 3), 1:10]
gene_totals <- bsd$col_sums()  # streamed, no full materialization
}
}
\section{Files with deleted cells}{
Backed handles address rows by \emph{physical} index and have no
kept-to-global translation, so opening a file that carries deletion vectors
(\code{\link{scx_delete}}) would hand back cells that are supposed to be gone.
Rather than do that silently, this \strong{errors}. Use
\code{scx_open(path)$x_matrix()} or \code{\link{scx_query}}, which apply
deletions, or run \code{scx compact} to materialize them away first.
}

\seealso{
\code{\link{scx_open}}; the \code{$x_backed()} method on an
\code{ScxExperiment} is the equivalent accessor for an already-open file.
}
