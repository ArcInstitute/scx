% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lazy.R
\name{[.ScxLazyTransformed}
\alias{[.ScxLazyTransformed}
\title{Index a lazy-transformed dataset}
\usage{
\method{[}{ScxLazyTransformed}(x, i, j, ..., drop = FALSE)
}
\arguments{
\item{x}{An \code{ScxLazyTransformed}.}

\item{i}{Row (cell) selector: positive integers or a logical vector of length
\code{nrow(x)}. Missing selects all rows.}

\item{j}{Column (gene) selector applied to the returned matrix. Missing
selects all columns.}

\item{...}{Unused.}

\item{drop}{Ignored; a \code{dgCMatrix} is always returned.}
}
\value{
A transformed \code{dgCMatrix} (cells x genes).
}
\description{
Reads the requested rows (cells) from disk, applies the transform chain, and
returns a transformed \code{dgCMatrix} of cells x genes. Same indexing rules
as \code{\link{[.ScxBackedSparse}}: positive-integer and logical row indices,
fractional ones truncated as a \code{dgCMatrix} does, \code{NA} / non-finite
rejected; an optional column index is applied to the returned matrix. The
underlying \code{$read_rows()} / \code{$read_row_indices()} methods are
0-based and strict.
}
