% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmony.R
\name{RunHarmony_scx}
\alias{RunHarmony_scx}
\title{Seurat-compatible Harmony2 wrapper}
\usage{
RunHarmony_scx(
  object,
  group.by.vars,
  reduction = "pca",
  reduction.save = "harmony",
  dims.use = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{Seurat} object (S4).}

\item{group.by.vars}{Name of a \code{meta.data} column holding batch
labels. A length-1 character vector. (Multi-covariate support is
exposed at the Rust layer but not yet wired through this wrapper.)}

\item{reduction}{Name of the input reduction. Default \code{"pca"}.}

\item{reduction.save}{Name of the output reduction. Default
\code{"harmony"}.}

\item{dims.use}{Optional integer vector of component indices to use
(e.g. \code{1:30}). \code{NULL} uses all components.}

\item{...}{Forwarded to \code{\link[=scx_harmony_integrate]{scx_harmony_integrate()}}. Use to set
\code{n_clusters}, \code{theta}, \code{sigma}, \code{max_iter}, \code{random_state}, etc.}
}
\value{
The Seurat object with a new reduction added at
\code{object[[reduction.save]]}. Also returns invisibly so the function
composes cleanly in pipelines.
}
\description{
Extracts a dimensional reduction (e.g. \code{"pca"}) from a Seurat object,
runs \code{\link[=scx_harmony_integrate]{scx_harmony_integrate()}} using a \code{meta.data} column as the batch
variable, and writes the corrected embeddings back as a new reduction
(default \code{"harmony"}). This mirrors \code{harmony::RunHarmony} so existing
Seurat pipelines can swap it in with minimal changes.
}
