% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accel.R
\name{scx-accelerators}
\alias{scx-accelerators}
\alias{scx_pca}
\alias{scx_pflog1ppf}
\alias{scx_neighbors}
\alias{scx_umap}
\alias{scx_leiden}
\alias{scx_rank_genes_groups}
\alias{scx_highly_variable_genes}
\title{Rust-native analysis accelerators for R}
\usage{
scx_pca(
  object,
  assay = NULL,
  layer = "data",
  features = NULL,
  n_components = 50L,
  zero_center = TRUE,
  n_oversamples = 10L,
  n_power_iterations = 2L,
  seed = 0L,
  reduction.name = "pca",
  reduction.key = "PC_"
)

scx_pflog1ppf(
  object,
  assay = NULL,
  layer = "counts",
  c = 1,
  n_components = 50L,
  zero_center = TRUE,
  n_oversamples = 10L,
  n_power_iterations = 2L,
  seed = 0L,
  reduction.name = "pflog1ppf",
  reduction.key = "PFLOG1PPF_"
)

scx_neighbors(
  object,
  reduction = "pca",
  dims = 1:30,
  k = 20L,
  ef_construction = 200L,
  ef_search = 200L,
  seed = 0L,
  assay = NULL,
  graph.name.prefix = NULL
)

scx_umap(
  object,
  reduction = "pca",
  dims = 1:30,
  n_neighbors = 15L,
  n_components = 2L,
  n_epochs = 200L,
  min_dist = 0.1,
  spread = 1,
  seed = 0L,
  assay = NULL,
  reduction.name = "umap",
  reduction.key = "UMAP_",
  neighbors = NULL
)

scx_leiden(
  object,
  reduction = "pca",
  dims = 1:30,
  n_neighbors = 20L,
  resolution = 1,
  seed = 0L,
  max_iterations = 100L,
  assay = NULL,
  neighbors = NULL
)

scx_rank_genes_groups(
  object,
  group.by = NULL,
  assay = NULL,
  layer = "data",
  features = NULL,
  groups = NULL,
  reference = NULL,
  log_transformed = TRUE,
  rankby_abs = FALSE,
  tie_correct = FALSE
)

scx_highly_variable_genes(
  object,
  assay = NULL,
  layer = "counts",
  n_top_genes = 2000L,
  span = 0.3
)
}
\arguments{
\item{object}{A \code{Seurat} object or a genes x cells \code{dgCMatrix}.}

\item{assay}{Assay to pull from (default: the object's default assay).}

\item{layer}{Layer to decompose (default \code{"data"}, i.e. log-normalized).}

\item{features}{Genes to use (default: \code{VariableFeatures(object)}, or all
genes for a matrix input).}

\item{n_components}{Number of principal components (default \code{50}).}

\item{zero_center}{Mean-center genes before SVD (default \code{TRUE}).}

\item{n_oversamples, n_power_iterations}{Randomized-SVD accuracy knobs.}

\item{seed}{RNG seed (default \code{0}).}

\item{reduction.name, reduction.key}{Output reduction slot name / key.}

\item{c}{Shift / pseudocount (default \code{1} for PFlog1pPF).

Operates on \strong{raw counts} (hence \code{layer = "counts"}), not log-normalized
data: counts are converted to within-cell proportions, shifted by \code{c},
log-transformed, then centered within each cell. The exact transform is
dense but decomposes into a sparse \code{delta} plus a per-cell \code{baseline}, so
PCA never densifies. For a \code{Seurat} object the embedding is written to the
\code{reduction.name} reduction and the per-cell baseline to
\code{object$pflog1ppf_baseline}. This is the \strong{in-memory} R path; the
streaming / atlas-scale out-of-core path is \code{pyscx}-only.

Unlike \link{scx_pca}, this does \strong{not} accept a \code{features} argument: PFlog1pPF's
per-cell depth \code{s_i} and centering denominator \code{D} are defined over the full
transcriptome, so subsetting to variable features before the transform would
silently change the statistic (and the stored \code{baseline}). The transform and
its PCA are always computed over the full counts layer.}

\item{reduction}{Input reduction (default \code{"pca"}).}

\item{dims}{Reduction dimensions to use (default \code{1:30}).}

\item{k}{Number of neighbors (default \code{20}).}

\item{ef_construction, ef_search}{HNSW accuracy knobs.}

\item{graph.name.prefix}{Prefix for the written graph slots.}

\item{n_neighbors}{Neighbors for the internal kNN build (default \code{15}).}

\item{n_epochs}{SGD epochs (default \code{200}).}

\item{min_dist, spread}{UMAP layout parameters.}

\item{neighbors}{Optional precomputed kNN result from \code{\link[=scx_neighbors]{scx_neighbors()}}
(matrix form). When supplied, its connectivity graph is reused; when
\code{NULL}, \code{scx_leiden} builds its \strong{own} kNN (\code{n_neighbors}) and does
\strong{not} read any graph slot written by \code{scx_neighbors()}.}

\item{resolution}{Resolution parameter (default \code{1.0}).}

\item{max_iterations}{Maximum Leiden outer iterations (default \code{100}).}

\item{group.by}{\code{meta.data} column with per-cell group labels.}

\item{reference}{Reference group, or \code{NULL} to test each group vs rest.}

\item{log_transformed}{Whether \code{layer} is already log-transformed.}

\item{rankby_abs, tie_correct}{Wilcoxon options; defaults (\code{FALSE}/\code{FALSE})
match \code{pyscx}/scanpy. The kernel densifies the matrix internally, so on a
Seurat object with \code{features = NULL} this defaults to \code{VariableFeatures()}
when set; passing the full gene set materializes a dense matrix.}

\item{n_top_genes}{Number of HVGs to select (default \code{2000}).}

\item{span}{Loess span for the mean-variance fit (default \code{0.3}).}
}
\value{
For DE, a long-format \code{data.frame} with columns \code{group}, \code{gene},
\code{score}, \code{pval}, \code{pval_adj}, \code{logfoldchange}.
}
\description{
Pipe-friendly front ends over the \code{scx-accel} CPU kernels, mirroring the
Seurat verbs they replace. Each function accepts \strong{either} a \code{Seurat}
object (results are written back into the slot Seurat expects and the
object is returned invisibly) \strong{or} a raw genes x cells \code{dgCMatrix} /
embedding matrix (the raw result list is returned). The matrix form makes
the kernels usable without Seurat installed.
}
\details{
The full pipeline is reachable from R:
Normalize -> \code{scx_highly_variable_genes} -> \code{scx_pca} -> \code{scx_neighbors}
-> \code{scx_umap} -> \code{scx_leiden} -> \code{scx_rank_genes_groups}.
}
\section{Functions}{
\itemize{
\item \code{scx_pca()}: PCA via randomized / covariance SVD.

\item \code{scx_pflog1ppf()}: PFlog1pPF / shifted-CLR normalization
(Booeshaghi et al. 2026) returning a baseline-aware PCA embedding.

\item \code{scx_neighbors()}: kNN graph (HNSW) from an embedding.

\item \code{scx_umap()}: UMAP embedding from a kNN graph.

\item \code{scx_leiden()}: Leiden clustering from a kNN graph.

\item \code{scx_rank_genes_groups()}: Wilcoxon rank-sum DE (FindAllMarkers analog).

\item \code{scx_highly_variable_genes()}: Highly variable genes (seurat_v3 / vst).

}}
\section{Note}{

The mean/variance and clipped-sum passes run in Rust (identical to the
Python accelerator), but the loess fit uses R's native \code{stats::loess}
whereas \code{pyscx} uses \code{skmisc.loess}. The two LOESS implementations can give
slightly different fitted values, so the selected HVG set may differ
marginally between R and Python on the same data.
}

