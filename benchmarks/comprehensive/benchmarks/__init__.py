# Benchmark modules for the comprehensive benchmark suite.
#
# Each module exposes a run(dataset, format_variant, n_runs, cold_cache) function
# returning a BenchmarkResult. Modules are loaded dynamically by run_all.py.
